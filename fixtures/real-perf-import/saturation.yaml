schemaVersion: 1
defaults:
  saturatedWhen:
    - metric_id: throttling
      stat: max
      operator: ">"
      value: 20
overrides:
  - run_id: "202605210458"
    saturated: true
    reason: "Kafka CPU decreased oddly."

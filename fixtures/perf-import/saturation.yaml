schemaVersion: 1
defaults:
  saturatedWhen:
    - metric_id: throttling
      stat: max
      operator: ">"
      value: 20
overrides:
  - run_id: run-004
    saturated: false
    reason: Investigated throttling spike was isolated to startup and did not affect steady-state throughput.
